name: 该接口的作用简介
variables:
  test: 默认数据，如果为数字，需要用双引号
  testName: 默认数据，如果为数字，需要用双引号

request:
  url: $uri/zbcf-casecenter/public/case/cancelCase
  method: POST
  headers:
    Content-Type: application/json
  json:
    test: $test  # 该字段的简介
    testName: $testName  # 该字段的简介